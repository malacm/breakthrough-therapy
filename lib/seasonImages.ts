/**
 * Fixed image paths for the hero and approach sections.
 */

export function getHeroImageUrl(): string {
  return '/images/hero.jpg';
}

export function getApproachImageUrl(): string {
  return '/images/section.jpg';
}
